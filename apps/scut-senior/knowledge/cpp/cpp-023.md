---
source_id: cpp-023
course_id: cpp
title: "签到"
original_file: "学科资料/C++（上及下）/C++代码合辑(Ai盛行，故停止维护)/algorithm/签到2.C++"
document_role: exercise_solution
year:
locator_type: none
---

# 签到

```cpp
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int t;
    cin >> t;
    vector<pair<long long, long long>> intervals(t);
    
    // 读入所有区间
    for (int i = 0; i < t; i++) {
        cin >> intervals[i].first >> intervals[i].second;
    }
    
    // 按右端点排序
    sort(intervals.begin(), intervals.end(), 
        [](const pair<long long, long long>& a, const pair<long long, long long>& b) {
            return a.second < b.second;
        });
    
    // 贪心选择
    int count = 0;
    long long lastEnd = -1e18;
    
    for (const auto& interval : intervals) {
        if (interval.first >= lastEnd) {
            count++;
            lastEnd = interval.second;
        }
    }
    
    cout << count << endl;
    return 0;
}

```
