---
source_id: embedded-systems-015
course_id: embedded_systems
title: "嵌入式2024级回忆版"
original_file: "学科资料/嵌入式/往年卷/嵌入式2024级回忆版.docx"
document_role: note
year: 2024
locator_type: none
---

# 嵌入式2024级回忆版

**诚信应考,考试作弊将带来严重后果！**

**华南理工大学期末考试**

**2024级《嵌入式系统》B卷回忆版**

**注意事项：** 本卷子为回忆版，所以不太全面，仅供参考！作者（GuMianQAQ）
- 填空题
1. ADC模块的作用是_______。现有某4位ADC模块，参考电压范围为0-5V，输出数字量为8，对应输入电压大约是_____。
1. 在STM32F103中，EXTI支持上升沿，下降沿，双边沿触发，若将一个GPIO引脚设置为上升沿，则中断会在____时刻产生。同一时刻多个外部中断同时发生时，由_____决定响应中断的先后顺序。
1. STM32F103中的DMA支持三种传输方向，分别为_______，与中断方式相比，DMA____(此处选需要or不需要)CPU干预数据搬运，适合____（此处选批量or随机）数据传输。
1. 某STM32F103定时器配置为外部时钟模式2，时钟源来自TIMx_ETR引脚。已知ETR引脚输入的外部信号频率为10kHz，经过ETR预分频器（分频系数为2）后，定时器实际计数时钟频率为____kHz。若该定时器工作在上行计数模式，且自动重装载寄存器ARR的值配置为1000-1，则定时器产生溢出中断的时间间隔为____ms。
1. 在嵌入式系统中，SysTick定时器的作用是____。
1. MOV   R0, #0x400

ORR       R1, R0, #0x900

MOV   R2, #0x12345678

STR       R2, [R0]

LDRB  R3, [R0, #2]

小端模式下，执行上述  ARM  指令后：

1.  地址  0x400 ~ 0x403  的  16  进制数存储为？

2. R1、R3  的值分别是多少？
- **选择题（来不及抄选项啦！自己跟着题目的考点复习！）**
1. SPI的总线特点是？
1. 在STM32的NVIC中断优先级配置中，若将优先级分组设置为3位抢占优先级和1位响应优先级，则抢占优先级的有效数值范围是？
1. STM32F103系列微控制器支持三种启动存储介质，它们分别是？
1. 某STM32定时器的输入时钟频率为48MHz，预分频器（PSC）寄存器值配置为4800-1，则计数器（CNT）每递增1次所对应的时间间隔为____ms。
1. 若要使通用定时器输出  PWM  波形，通常需要配置哪些主要寄存器？
1. IWDG的时钟源是？
1. CPSR的N位为1表示？
1. GPIO  引脚的输出速率应如何配置？
1. 某  PWM  输出信号的周期  T = 2  ms，高电平电压为  3.3 V，低电平电压为  0 V。若在一个周期内高电平持续时间为  0.8  ms，则该  PWM  信号的平均输出电压约为多少？
1. I²C接口的特点是？

**三．**简答题

1．结合嵌入式微控制器的特点，与PC相比嵌入式系统在功耗、可靠性和专用性

方面有哪些特定设计考虑

2．说明  STM32F103  定时器时基单元主要由哪些模块组成，并简要说明各模块

的作用。

**3．**解释为什么STM32F103的USADT通信需要设置波特率，并说明波特率误差过大会导致什么问题。

4．简述STM32F103 如何测量一个外部方波的脉冲宽度，并在测量中如何处理计数器溢出的情况

5.  说明嵌入式操作系统中“信号量”的主要功能，说明至少2项其适用的场景。

**四．**综合题

1某  STM32F103  系统有两个输入信号和一个输出信号。

输入一：开关  S  接在  PC6  引脚上，开关闭合时  PC6  为高电平，开关断开时  PC6  为低电平。

输入二：上位机  PC  通过  USART2  向单片机发送串口数据。当  USART2  接收到字符  'c'  时，认为串口输入有效。USART2  通信参数设置为：波特率  115200，8  位数据位，1  位停止位，偶校验，无硬件流控制，仅接收模式。

输出：LED  接在  PB5  引脚上，PB5  输出高电平时  LED  点亮，输出低电平时  LED  熄灭，GPIO  输出速率配置为低速。

要求实现如下逻辑功能：

当开关  S  闭合，即  PC6  为高电平，且  USART2  接收到字符  'c'  时，LED  点亮；否则  LED  熄灭。
1. 编写void  GPIO_Init(void)，配置PC6接下拉电阻，并说明其作用。
1. 编写  void USART2_Init(void)，完成  USART2  初始化。
1. 编写  int main(void)，实现  PC6  为高电平且  USART2  收到字符  'c'  时  LED  点亮，否则  LED  熄灭。
1. RTOS分类讨论

用嵌入式操作系统实现相同功能，回答需要创建几个任务？分别描述每个任务的功能。如何实现任务间关系处理以满足题目要求的LED控制？请简要说明一个嵌入式操作系统可行机制。

2.（这里真的是回忆版了！快到时间了没办法QAQ）

1.同样是编写void  GPIO_Init(void)，但要求配置抢占优先级为1和响应优先级为0，并且需要使能中断。

2.编写中断回调函数。

3.用户编写的中断回调函数是否固定？简要说明HAL库是如何以基础中断处理函数调用到用户自定义的回调函数。
