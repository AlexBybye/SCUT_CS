[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "scut-senior-api"
version = "0.1.0"
description = "Iteration 0 contract and mock vertical slice for SCUT Senior"
requires-python = ">=3.12"
dependencies = [
  "cryptography>=45,<46",
  "fastapi>=0.116,<1",
  "idna>=3.18,<4",
  "jsonschema>=4.25,<5",
  "pydantic>=2.11,<3",
  "python-multipart>=0.0.20,<1",
  "PyYAML>=6,<7",
  "scut-senior-worker==0.1.0",
  "uvicorn[standard]>=0.35,<1",
]

[project.optional-dependencies]
dev = [
  "httpx>=0.28,<1",
  "pytest>=8.4,<9",
  "pytest-cov>=6.2,<7",
]
onnx = [
  "onnxruntime>=1.18,<2",
  "tokenizers>=0.19,<1",
]

[project.scripts]
scut-senior-eval = "scut_senior_api.eval_runner:main"
scut-senior-build-vectors = "scut_senior_api.vector_index:main"

[tool.hatch.build.targets.wheel]
packages = ["src/scut_senior_api"]

[tool.uv.sources]
scut-senior-worker = { path = "../worker", editable = true }

[tool.pytest.ini_options]
addopts = "-ra --strict-markers"
pythonpath = ["src", "../worker/src"]
testpaths = ["../tests/python"]

[tool.coverage.run]
branch = true
source = ["scut_senior_api", "scut_senior_worker"]
