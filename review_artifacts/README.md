# 公开审核证据

本目录只保存已经脱敏、确有必要公开的人工审核记录。转换输入副本、插件粗稿、`*_debug.html`、临时图片目录、转换脚本和其他 staging 产物只用于本地审核，不进入 Git。

课程知识事实源仍是 `knowledge/manifest.csv` 中状态为 `passed` 的记录、对应规范化 Markdown 和必要 assets。审核记录不能代替 manifest 状态、原件对照或 corpus candidate 校验。
